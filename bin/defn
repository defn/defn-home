#!/usr/bin/env bash

export BOX_NAME='box'
export BASEBOX_NAME='defn'
export BASEBOX_HOME_URL='git@github.com:defn/home'

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  exec limbo "$@"
}

source sub "$BASH_SOURCE" "$@"
