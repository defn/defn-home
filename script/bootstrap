#!/usr/bin/env bash

function home_bootstrap {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"

  source "$shome/script/profile"
  "$shome/script/update"
  "$shome/script/build"
}

home_bootstrap "$@"
