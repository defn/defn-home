#!/usr/bin/env bash

function profile {
  local shome="${_defn_home_home:-"$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"}"
  PATH="$shome/bin:$shome/exec:$PATH"

  export SVDIR="$shome/service"

  source "$shome/script/rc"
}

profile
