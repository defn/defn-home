#!/usr/bin/env bash

function home_cibuild {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"

  mkdir -p "$shome/work"
  pushd "$shome/work"
  git clone git@github.com:defn/block || true
  script/cibuild "$@"
  popd
}

home_cibuild "$@"
