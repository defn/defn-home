#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  block compile service
  VBoxManager hostonlyif ipconfig vboxnet0 --ip 172.28.128.1
}

main "$@"
