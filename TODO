cat .ssh/authorized_keys | vagrant ssh -- tee -a .ssh/authorized_keys

accept github.com, localhost ssh host keys

git clone git@github.com:defn/home

mv home/.git .
rm -rf home
git checkout .
git clean -fd

script/bootstrap
source script/profile
require
app bootstrap

rsync -ia --progress packages distfiles cache
