#!/usr/bin/env bash

function home_pkg {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"

  export PKG_HOME="$shome"

  if [[ -f "$shome/.profile.d/profile.pre-secret" ]]; then
    source "$shome/.profile.d/profile.pre-secret"
  fi
}

home_pkg
